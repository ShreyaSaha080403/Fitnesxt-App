import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FullboodyworkPage } from './fullboodywork.page';

describe('FullboodyworkPage', () => {
  let component: FullboodyworkPage;
  let fixture: ComponentFixture<FullboodyworkPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FullboodyworkPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

